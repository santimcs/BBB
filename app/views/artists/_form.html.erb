<%= form_with(model: artist, local: true) do |form| %>
  <% if artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :artist_name %>
  </div>

  <div class="field">
    <%= form.label :chosen %><br>
    <%= form.check_box :chosen %>
  </div>

  <div class="field">
    <%= form.label :charted_songs %>
    <%= form.number_field :charted_songs, id: :artist_charted_songs %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image, id: :artist_image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
